[package]
name = "compactador-windows-integration"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "compactador_windows_integration"
path = "src/lib.rs"

[dependencies]
compactador-core.workspace = true

[target.'cfg(windows)'.dependencies]
winreg = "0.52"
